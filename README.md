# WPF Project — Система управления пользователями и платежами по категориям

## 1. Описание проекта

Приложение представляет собой настольную информационную систему для управления пользователями и их платежами по категориям.
Проект разработан с использованием технологии **Windows Presentation Foundation (WPF)** и **Entity Framework** для взаимодействия с базой данных **Microsoft SQL Server**.

Функциональные возможности:

* Авторизация и регистрация пользователей
* Добавление, редактирвание и удаление данных в таблицах
* Выгрузка данных в форматах .docx и .excel 
* Поиск пользователей по ФИО
* Сортировка данных по алфавиту
* Фильтрация по ролям (администратор, пользователь)
* Переключение между светлой и тёмной темой интерфейса
* Отображение фотографий пользователей с заглушкой при отсутствии изображения
* Отображение статистики и информации о платежах

---

## 2. Используемые технологии

| Компонент                  | Описание                           |
| -------------------------- | ---------------------------------- |
| Язык программирования      | C# (.NET Framework / .NET 6)       |
| Пользовательский интерфейс | WPF (XAML)                         |
| ORM                        | Entity Framework                   |
| База данных                | Microsoft SQL Server               |
| Среда разработки           | Visual Studio 2022                 |
| Архитектура приложения     | Частичная реализация паттерна MVVM |

---

## 3. Настройка проекта

1. Клонировать репозиторий проекта:

   ```bash
   git clone https://github.com/titorenko-rita/222_Titorenko.git
   ```
2. Открыть файл решения `.sln` в среде **Visual Studio**.
3. Убедиться, что в файле `App.config` корректно указана строка подключения к базе данных:

   ```xml
   <connectionStrings>
       <add name="WPF_ProjectEntities" 
            connectionString="metadata=res://*/Models.Model.csdl|res://*/Models.Model.ssdl|res://*/Models.Model.msl;
                              provider=System.Data.SqlClient;
                              provider connection string=&quot;data source=.\SQLEXPRESS;
                              initial catalog=WPF_Project;
                              integrated security=True;MultipleActiveResultSets=True;&quot;"
            providerName="System.Data.EntityClient" />
   </connectionStrings>
   ```
4. Выполнить миграции или обновление базы данных через **Entity Framework**.
5. Запустить проект (клавиша `F5`).

---

## 4. Основные элементы интерфейса

* **Главное окно:** содержит элементы навигации, кнопки переключения тем и область отображения страниц.
* **Страница админа (AdminPage):** отображает список пользователей в виде карточек с фотографией, ФИО и ролью.
* **Страница пользователей (UserPage):** отображает список пользователей в виде карточек с фотографией, ФИО и ролью.
* **Панель фильтров:** включает поле поиска, выбор сортировки и фильтр по ролям.
* **Темы оформления:** реализованы две темы — светлая и тёмная, с автоматическим изменением цвета текста и элементов интерфейса.

---

## 5. Скриншоты интерфейса

### Тёмная тема

<img width="1920" height="1080" alt="image" src="https://github.com/user-attachments/assets/ede7e2f8-8239-45dd-b5e1-7beeb1300e4e" />


### Светлая тема

<img width="1920" height="1080" alt="image" src="https://github.com/user-attachments/assets/a5e45691-124d-4525-a4d1-cbd52c5518da" />


---

## 6. Известные ошибки и их решения

**Ошибка 1.**

```
A specified Include path is not valid. The EntityType 'Payment' does not declare a navigation property with the name 'Users'.
```

**Причина:** В модели Entity Framework указано неверное имя навигационного свойства при использовании метода `Include()`.
**Решение:** Проверить модель данных и использовать корректное свойство навигации, например:

```csharp
context.Payments.Include(p => p.User)
```

---

**Ошибка 2.**

```
Не удалось создать "ImageSource" на основе текста "images/default.png"
```

**Причина:** Относительный путь к изображению указан некорректно.
**Решение:** Использовать абсолютный URI-формат для встроенного ресурса:

```xml
<Image Source="pack://application:,,,/images/default.png"/>
```

или убедиться, что папка `images` помечена как *Resource* в свойствах проекта.

---

## 7. Автор

**Титоренко Маргарита Игоревна**
Студент колледжа информационных технологий
Специальность: Разработка программных модулей

---

## 8. Структура проекта

📁 222_Titorenko
│
├── 📁 Properties
│   └── AssemblyInfo.cs — общие сведения о сборке проекта.
│
├── 📁 images
│   └── default.png — изображение-заглушка для пользователей без фотографии.
│
├── 📁 Pages
│   ├── AddCategoryPage.xaml / .cs — страница добавления новой категории.
│   ├── AddPaymentPage.xaml / .cs — страница добавления или редактирования платежей.
│   ├── AddUserPage.xaml / .cs — страница регистрации нового пользователя (доступна администратору).
│   ├── AdminPage.xaml / .cs — панель администратора, содержащая ссылки на вкладки.
│   ├── AuthPage.xaml / .cs — страница авторизации пользователей.
│   ├── CategoryTabPage.xaml / .cs — вкладка с перечнем категорий платежей.
│   ├── ChangePassPage.xaml / .cs — страница для смены пароля пользователя.
│   ├── DiagrammPage.xaml / .cs — страница для отображения статистических диаграмм.
│   ├── PaymentTabPage.xaml / .cs — вкладка, содержащая таблицу платежей с фильтрацией и удалением.
│   ├── RegPage.xaml / .cs — страница регистрации новых пользователей.
│   ├── UserPage.xaml / .cs — страница отображения списка пользователей в виде карточек с фотографиями.
│   └── UsersTabPage.xaml / .cs — административная таблица пользователей (для редактирования и удаления).
│
├── App.xaml / App.xaml.cs — определение ресурсов приложения, стилей и обработка событий запуска.
│
├── App.config — файл конфигурации, содержащий строку подключения к базе данных.
│
├── DBConnect.cs — класс, реализующий шаблон Singleton для доступа к контексту базы данных.
│
├── Dictionary.xaml — словарь ресурсов (светлая тема).
│
├── DictionaryDark.xaml — словарь ресурсов (тёмная тема).
│
├── ico4825231.ico — иконка приложения.
│
├── MainWindow.xaml / .cs — главное окно приложения, содержащее навигацию, переключение тем и время.
│
├── Model1.edmx — модель данных Entity Framework, связывающая приложение с базой данных.
│
└── packages.config — список NuGet-пакетов, используемых в проекте (EntityFramework и др.).
